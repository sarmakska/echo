[package]
name = "echo-brain"
version = "0.1.0"
edition = "2021"
description = "Echo brain workers: subscription-backed CLI wrappers"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
