[package]
name = "echo-skills"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
ureq = { version = "2", optional = true }

[features]
net = ["dep:ureq"]
