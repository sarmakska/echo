[package]
name = "echo-memory"
version = "0.1.0"
edition = "2021"
description = "Echo file-based memory store"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
